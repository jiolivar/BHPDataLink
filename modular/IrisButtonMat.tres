[gd_resource type="ShaderMaterial" load_steps=2 format=2]

[sub_resource type="Shader" id=2]
code = "shader_type canvas_item;

void vertex() {
    if (UV.y > 0.5){
		VERTEX.y -= (1.0 - UV.x) * 12.0;
	}else{
		VERTEX.y += (1.0 - UV.x) * 12.0;
	}
}"

[resource]
render_priority = 1
shader = SubResource( 2 )
